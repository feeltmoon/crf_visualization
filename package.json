{
  "name": "formcraft-als-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Static ALS CRF dashboard with a minimal Heroku web server",
  "engines": { "node": "24.x" },
  "scripts": { "start": "node server.cjs", "test": "node --test server.test.cjs" }
}
